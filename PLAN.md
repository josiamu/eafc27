# แผนงาน EAFC Skill Hub

เว็บสอนสกิลมูฟและเพลย์สไตล์ EA FC สองภาษา ผู้อ่านตั้งค่าปุ่มจอยครั้งเดียว แล้วทั้งเว็บแสดงปุ่มตามจอยที่เขาถืออยู่จริง

- แผนฉบับหน้าเว็บ: https://claude.ai/code/artifact/68cf09c6-5c0b-499d-abc3-1faacdeb6be8
- สร้างเมื่อ: 10 กันยายน 2026
- สถานะ: ระยะ 1–3 เสร็จ พร้อมท่าตัวอย่าง 5 ท่า (16 ก.ย. 2026) ยังไม่ได้ทดสอบบนมือถือจริง

---

## ข้อสรุปที่ตกลงกันแล้ว

| หัวข้อ | ข้อสรุป |
|---|---|
| เทคโนโลยี | Next.js + TypeScript + Tailwind, static export |
| ภาษา | ไทยเป็นค่าตั้งต้น สลับอังกฤษได้ทุกหน้า |
| ปลายทาง | GitHub Pages ไม่มีหลังบ้าน |
| อุปกรณ์ | มือถือและคอมพิวเตอร์เท่ากัน responsive เต็ม |
| ขอบเขตรอบแรก | Skill Moves และ PlayStyles เท่านั้น |
| ข้อมูลตั้งต้น | อ้างอิง EA FC 26 พร้อมป้ายเตือนในเว็บ |
| วิดีโอ | เว้นช่องไว้ทุกท่า เติมลิงก์ทีหลังได้ |
| URL | `josiamu.github.io/eafc27` (repo `eafc27`, basePath `/eafc27`) |
| แหล่งข้อมูล | ค้นเว็บเทียบหลายแหล่ง ทุกท่ามีช่อง `sources` และธง `verified` |
| ธีม | ตามระบบ พร้อมปุ่มสลับสว่าง/มืดที่จำค่าไว้ |
| ขอบเขตรอบนี้ | ระยะ 1–3 พร้อมท่าตัวอย่าง แล้วหยุดให้รีวิว |

---

## หลักสามข้อที่ทั้งเว็บยึด

1. **เนื้อหาอยู่ใน JSON ไม่ปนกับโค้ด** ทุกท่าและทุกเพลย์สไตล์เก็บเป็นไฟล์ข้อมูลที่ตรวจด้วย schema ตอน build ถ้าพิมพ์ชื่อปุ่มผิดหรือใส่ดาวเกินห้า จะ build ไม่ผ่าน
2. **ปุ่มถูกเก็บเป็นชื่อกลาง** ในไฟล์ข้อมูลเขียนว่า `SHOULDER_R1` ไม่ใช่ R1 หรือ RB แล้วค่อยแปลตอนแสดงผล นี่คือสิ่งที่ทำให้หน้าตั้งค่ากับหน้าสกิลผูกกันจริง
3. **จำค่าไว้ในเครื่องผู้อ่าน** preset จอย ปุ่มที่แก้เอง และภาษาที่เลือก เก็บใน localStorage ไม่ต้องล็อกอิน

---

## ระยะที่ 1 — วางฐาน

เป้าหมาย: มีเว็บเปล่าที่ deploy ขึ้น GitHub Pages ได้จริงตั้งแต่วันแรก

- [x] `npx create-next-app@latest` พร้อม TypeScript, Tailwind, App Router
- [x] ตั้ง `output: 'export'` และ `basePath` ให้ตรงกับชื่อ repo ใน `next.config.ts`
- [x] เพิ่มไฟล์ `.nojekyll` ใน `public/`
- [x] วางโครง route แบบ `app/[locale]/...` รองรับ `th` และ `en`
- [x] ทำ dictionary สองภาษา และตัวสลับภาษาที่จำค่าไว้
- [x] ตั้ง `generateStaticParams` ให้ export ทั้งสองภาษา
- [x] ตั้ง GitHub Actions ให้ build แล้ว deploy ขึ้น Pages อัตโนมัติ
- [x] ตั้งฟอนต์ที่รองรับภาษาไทย และ palette หลักของเว็บ

**เสร็จเมื่อ** เปิด URL ของ GitHub Pages แล้วเห็นหน้าแรกภาษาไทย และสลับไปอังกฤษได้

---

## ระยะที่ 2 — ระบบปุ่มจอย

เป้าหมาย: ตั้งค่าปุ่มครั้งเดียว แล้วทุกหน้าที่แสดงปุ่มเปลี่ยนตาม

- [x] นิยาม type ของชื่อปุ่มกลางให้ครบ เช่น `SHOULDER_R1`, `TRIGGER_L2`, `FACE_BOTTOM`, `STICK_R_FLICK`
- [x] เขียน preset สามชุด PlayStation, Xbox, Nintendo Switch
- [x] ทำคอมโพเนนต์ `ButtonGlyph` ที่รับชื่อกลางแล้วแสดงไอคอนตาม preset ปัจจุบัน
- [x] ทำ context หรือ store เก็บ mapping ปัจจุบัน
- [x] เขียนตัวอ่านและเขียน localStorage ที่ทนกรณีไม่มีค่าเดิม
- [x] หน้า `/settings` ชั้นที่ 1 สลับ preset
- [x] หน้า `/settings` ชั้นที่ 2 แก้ไอคอน ชื่อเรียก และสี ของทุกปุ่ม พร้อมปุ่มรีเซ็ต
- [x] หน้า `/settings` ชั้นที่ 3 ผูกปุ่มจากจอยจริงผ่าน Gamepad API พร้อม fallback ถ้าเบราว์เซอร์ไม่รองรับ
- [x] ตัวสลับ preset แบบย่อบนแถบบนสุดของทุกหน้า

**เสร็จเมื่อ** เปลี่ยน preset แล้วไอคอนตัวอย่างเปลี่ยนทันที ปิดเบราว์เซอร์เปิดใหม่ยังได้ค่าเดิม

---

## ระยะที่ 3 — เครื่องมือแสดงท่า

เป้าหมาย: คอมโพเนนต์ชุดเดียวที่ทุกท่าใช้ร่วมกัน

- [x] `InputSequence` แถบลำดับปุ่ม บอกจังหวะค้างหรือแตะ
- [x] `PitchDiagram` สนาม SVG นักเตะซ้าย ประตูขวา ทิศทางบุกไปทางขวาเสมอ
- [x] `MoveAnimation` เล่นวนซ้ำ ปุ่มสว่างทีละขั้นพร้อมนักเตะขยับ
- [x] ปุ่มชะลอความเร็วและปุ่มเดินทีละขั้น
- [x] `VideoSlot` ถ้าไม่มีลิงก์ให้ซ่อนช่องไว้เงียบ ๆ
- [x] รองรับ `prefers-reduced-motion`
- [ ] ตรวจว่าทั้งหมดย่อลงจอมือถือได้โดยไม่ล้น (ย่อหน้าต่างเบราว์เซอร์ทดสอบไม่ได้ ต้องลองบนมือถือจริง)

**เสร็จเมื่อ** ใส่ข้อมูลท่าตัวอย่างหนึ่งท่าแล้วได้ครบทั้งสี่ชั้น และเปลี่ยน preset จอยแล้วไดอะแกรมเปลี่ยนตาม

---

## ระยะที่ 4 — ใส่ข้อมูล

ทำคู่ขนานกับระยะที่ 3 ได้

- [x] ออกแบบ schema ของท่า: slug, ชื่อไทย, ชื่ออังกฤษ, ดาว, ลำดับปุ่ม, สถานการณ์ที่ใช้, ความยาก, เพลย์สไตล์ที่เสริม, ท่าต่อเนื่อง, ลิงก์วิดีโอ
- [ ] ออกแบบ schema ของเพลย์สไตล์: slug, ชื่อสองภาษา, หมวด, ผลที่ได้, วิธีใช้จริง, ท่าที่มันเสริม
- [x] เขียนตัวตรวจ schema ที่รันตอน build และ fail ถ้าข้อมูลผิด (ของท่า ส่วนเพลย์สไตล์ยังไม่ทำ)
- [x] ใส่ท่าระดับ 1 ดาว และ 2 ดาว
- [x] ใส่ท่าระดับ 3 ดาว
- [x] ใส่ท่าระดับ 4 ดาว
- [x] ใส่ท่าระดับ 5 ดาว
- [ ] ใส่เพลย์สไตล์ครบทุกหมวด ยิง จ่าย ควบคุมบอล ป้องกัน ร่างกาย ผู้รักษาประตู
- [ ] ตรวจว่าลิงก์ระหว่างท่ากับเพลย์สไตล์ชี้ถูกทั้งสองทาง

**ปริมาณโดยประมาณ** สกิลมูฟราวแปดสิบท่า เพลย์สไตล์ราวสามสิบสี่อัน

**ความคืบหน้า (16 ก.ย. 2026)** ใส่สกิลมูฟแล้ว 58 ไฟล์ (รวมคู่ซ้าย/ขวาเป็นไฟล์เดียว เทียบได้ราว 80 ท่าในคู่มือ) ยืนยันแล้ว 52 รอตรวจ 6 ยังไม่ใส่ท่าเดาะบอลกลางอากาศ 5 ดาว (มีแหล่งเดียว) และยังไม่เริ่มเพลย์สไตล์

---

## ระยะที่ 5 — ประกอบหน้า

- [ ] `/` หน้าแรก ชวนเลือก preset จอยก่อนเริ่มใช้
- [ ] `/skills` ภาพรวมทุกดาว
- [ ] `/skills/1-star` ถึง `/skills/5-star` ลิสต์ท่าแยกตามดาว
- [ ] `/skills/[slug]` หน้ารายท่า
- [ ] `/playstyles` รวมทุกเพลย์สไตล์แยกตามหมวด
- [ ] `/playstyles/[slug]` หน้ารายอัน ย้อนลิงก์กลับไปยังท่าที่มันเสริม
- [ ] ช่องค้นหาและตัวกรองตามดาวและสถานการณ์
- [ ] ลิงก์ท่าต่อเนื่องและท่าคล้ายกันในหน้ารายท่า

**เสร็จเมื่อ** เดินอ่านวนได้ทั้งจากท่าไปหาเพลย์สไตล์และย้อนกลับ โดยไม่เจอลิงก์ตาย

---

## ระยะที่ 6 — เก็บงาน

- [ ] ตรวจการแสดงผลบนมือถือจริง ไม่ใช่แค่ย่อหน้าต่างเบราว์เซอร์
- [ ] ใช้งานด้วยคีย์บอร์ดได้ มี focus ที่มองเห็น
- [ ] ตรวจ contrast ของสีทั้งธีมสว่างและธีมมืด
- [ ] ใส่ title, description, og image ของแต่ละหน้า ทั้งสองภาษา
- [ ] ป้ายบอกว่าข้อมูลอ้างอิง EA FC เวอร์ชันไหน แสดงในทุกหน้าเนื้อหา
- [ ] หน้าอธิบายที่มาของข้อมูลและวิธีแจ้งแก้ไข
- [ ] ตรวจว่า static export ไม่มีหน้าไหนพังเพราะ dynamic route

---

## ข้อควรทราบเรื่องข้อมูล

ข้อมูลชุดตั้งต้นเป็นของ EA FC 26 ที่ยืนยันได้ ไม่ใช่ FC 27 ท่าสกิลมูฟส่วนใหญ่มักไม่เปลี่ยนข้ามภาค แต่เพลย์สไตล์มีโอกาสถูกปรับหรือเพิ่มลด เว็บจะขึ้นป้ายบอกผู้อ่านให้ชัดว่าอ้างอิงภาคไหน และโครงสร้างไฟล์ข้อมูลรองรับการอัปเดตเป็น FC 27 ทีหลังโดยไม่ต้องรื้อโค้ด

## ลำดับที่ต้องเรียงกัน

ระยะ 1 → 2 → 3 ต้องทำตามลำดับ ระยะ 4 แทรกคู่ขนานกับระยะ 3 ได้ ระยะ 5 ต้องรอทั้ง 3 และ 4 เสร็จ
