# แผนงาน EAFC Skill Hub

เว็บสอนสกิลมูฟ EA FC สองภาษา ผู้อ่านตั้งค่าปุ่มจอยครั้งเดียว แล้วทั้งเว็บแสดงปุ่มตามจอยที่เขาถืออยู่จริง

- แผนฉบับหน้าเว็บ: https://claude.ai/code/artifact/68cf09c6-5c0b-499d-abc3-1faacdeb6be8
- สร้างเมื่อ: 10 กันยายน 2026
- สถานะ (16 ก.ย. 2026): ระยะ 1–5 เสร็จ (สกิลมูฟ 57 ท่า ยืนยันครบ) · ตัดเพลย์สไตล์ออกจากขอบเขต · โหมดฝึกกดจากจอยจริงเสร็จและผ่านการลองกับจอยจริงแล้ว · ระยะ 6 เหลือตรวจบนมือถือจริงข้อเดียว

---

## ข้อสรุปที่ตกลงกันแล้ว

| หัวข้อ | ข้อสรุป |
|---|---|
| เทคโนโลยี | Next.js + TypeScript + Tailwind, static export |
| ภาษา | ไทยเป็นค่าตั้งต้น สลับอังกฤษได้ทุกหน้า |
| ปลายทาง | GitHub Pages ไม่มีหลังบ้าน |
| อุปกรณ์ | มือถือและคอมพิวเตอร์เท่ากัน responsive เต็ม |
| ขอบเขต | Skill Moves เท่านั้น (ตัด PlayStyles ออกเมื่อ 16 ก.ย. 2026) |
| ข้อมูลตั้งต้น | อ้างอิง EA FC 26 พร้อมป้ายเตือนในเว็บ |
| วิดีโอ | เว้นช่องไว้ทุกท่า เติมลิงก์ทีหลังได้ |
| URL | `josiamu.github.io/eafc27` (repo `eafc27`, basePath `/eafc27`) |
| แหล่งข้อมูล | ค้นเว็บเทียบหลายแหล่ง ทุกท่ามีช่อง `sources` และธง `verified` |
| ธีม | ตามระบบ พร้อมปุ่มสลับสว่าง/มืดที่จำค่าไว้ |
| งานถัดไป | ลองหน้าที่เหลือบนมือถือจริง แล้วปิดระยะ 6 |

---

## หลักสี่ข้อที่ทั้งเว็บยึด

1. **เนื้อหาอยู่ใน JSON ไม่ปนกับโค้ด** ทุกท่าเก็บเป็นไฟล์ข้อมูลที่ตรวจด้วย schema ตอน build ถ้าพิมพ์ชื่อปุ่มผิดหรือใส่ดาวเกินห้า จะ build ไม่ผ่าน
2. **ปุ่มถูกเก็บเป็นชื่อกลาง** ในไฟล์ข้อมูลเขียนว่า `SHOULDER_R1` ไม่ใช่ R1 หรือ RB แล้วค่อยแปลตอนแสดงผล นี่คือสิ่งที่ทำให้หน้าตั้งค่ากับหน้าสกิลผูกกันจริง
3. **จำค่าไว้ในเครื่องผู้อ่าน** preset จอย ปุ่มที่แก้เอง และภาษาที่เลือก เก็บใน localStorage ไม่ต้องล็อกอิน
4. **คำนวณเอา อย่าเก็บซ้ำ** ท่าฝั่งตรงข้ามคำนวณจากท่าหลัก ไม่เก็บเป็นข้อมูลชุดที่สอง และพิกัดสนามเก็บในกรอบของท่า ส่วนการหันของภาพเป็นเรื่องของคอมโพเนนต์ที่วาด ของสองชุดที่ต้องตรงกันเสมอจะหลุดจากกันเมื่อไรก็ได้ ถ้ามีชุดเดียวก็ไม่มีอะไรให้หลุด

---

## ระยะที่ 1 — วางฐาน

เป้าหมาย: มีเว็บเปล่าที่ deploy ขึ้น GitHub Pages ได้จริงตั้งแต่วันแรก

- [x] `npx create-next-app@latest` พร้อม TypeScript, Tailwind, App Router
- [x] ตั้ง `output: 'export'` และ `basePath` ให้ตรงกับชื่อ repo ใน `next.config.ts`
- [x] เพิ่มไฟล์ `.nojekyll` ใน `public/`
- [x] วางโครง route แบบ `app/[locale]/...` รองรับ `th` และ `en`
- [x] ทำ dictionary สองภาษา และตัวสลับภาษาที่จำค่าไว้
- [x] ตั้ง `generateStaticParams` ให้ export ทั้งสองภาษา
- [x] ตั้ง GitHub Actions ให้ build แล้ว deploy ขึ้น Pages อัตโนมัติ
- [x] ตั้งฟอนต์ที่รองรับภาษาไทย และ palette หลักของเว็บ

**เสร็จเมื่อ** เปิด URL ของ GitHub Pages แล้วเห็นหน้าแรกภาษาไทย และสลับไปอังกฤษได้ ✅

---

## ระยะที่ 2 — ระบบปุ่มจอย

เป้าหมาย: ตั้งค่าปุ่มครั้งเดียว แล้วทุกหน้าที่แสดงปุ่มเปลี่ยนตาม

- [x] นิยาม type ของชื่อปุ่มกลางให้ครบ เช่น `SHOULDER_R1`, `TRIGGER_L2`, `FACE_BOTTOM`, `STICK_R_FLICK`
- [x] เขียน preset สามชุด PlayStation, Xbox, Nintendo Switch
- [x] ทำคอมโพเนนต์ `ButtonGlyph` ที่รับชื่อกลางแล้วแสดงไอคอนตาม preset ปัจจุบัน
- [x] ทำ context หรือ store เก็บ mapping ปัจจุบัน
- [x] เขียนตัวอ่านและเขียน localStorage ที่ทนกรณีไม่มีค่าเดิม
- [x] หน้า `/settings` เลือก preset
- [x] หน้า `/settings` แก้ไอคอน ชื่อเรียก และสี ของทุกปุ่ม พร้อมปุ่มรีเซ็ต
- [x] ผูกปุ่มจากจอยจริงผ่าน Gamepad API พร้อม fallback ถ้าเบราว์เซอร์ไม่รองรับ
- [x] ตัวสลับ preset แบบย่อบนแถบบนสุดของทุกหน้า
- [x] ปุ่มแต่ละปุ่มเรียกด้วยหน้าที่ในเกม (จ่ายบอล ยิง วิ่ง) ไม่ใช่ตำแหน่ง อิงคอนฟิก Classic จากสองแหล่งที่ตรงกัน
- [x] การผูกปุ่มเป็นการสลับที่เสมอ ปุ่มเดียวทำสองหน้าที่พร้อมกันไม่ได้
- [x] รวม "ปรับหน้าตาปุ่ม" กับ "ผูกปุ่มจากจอย" เป็นหัวข้อเดียว เลือกปุ่มแล้วแก้ได้ทั้งสองอย่าง

**เสร็จเมื่อ** เปลี่ยน preset แล้วไอคอนตัวอย่างเปลี่ยนทันที ปิดเบราว์เซอร์เปิดใหม่ยังได้ค่าเดิม ✅

---

## ระยะที่ 3 — เครื่องมือแสดงท่า

เป้าหมาย: คอมโพเนนต์ชุดเดียวที่ทุกท่าใช้ร่วมกัน

- [x] `InputSequence` แถบลำดับปุ่ม บอกจังหวะค้างหรือแตะ
- [x] `PitchDiagram` สนาม SVG บุกขึ้นบนเสมอ ทิศในข้อมูลจึงชี้ทางเดียวกับบนจอ
- [x] ครอบภาพเหลือเฉพาะบริเวณที่ทุกท่าเล่นจริง (x 46–95 จาก 160)
- [x] `MoveAnimation` เล่นวนซ้ำ ปุ่มสว่างทีละขั้นพร้อมนักเตะขยับ
- [x] ปุ่มชะลอความเร็วและปุ่มเดินทีละขั้น
- [x] `VideoSlot` ถ้าไม่มีลิงก์ให้ซ่อนช่องไว้เงียบ ๆ
- [x] รองรับ `prefers-reduced-motion`
- [x] ตรวจว่าทั้งหมดย่อลงจอมือถือได้โดยไม่ล้น (ลองบนมือถือจริงแล้ว 16 ก.ย. 2026)

**เสร็จเมื่อ** ใส่ข้อมูลท่าตัวอย่างหนึ่งท่าแล้วได้ครบทั้งสี่ชั้น และเปลี่ยน preset จอยแล้วไดอะแกรมเปลี่ยนตาม ✅

---

## ระยะที่ 4 — ใส่ข้อมูล

- [x] ออกแบบ schema ของท่า: slug, ชื่อไทย, ชื่ออังกฤษ, ดาว, ลำดับปุ่ม, สถานการณ์ที่ใช้, ความยาก, ท่าต่อเนื่อง, ลิงก์วิดีโอ
- [x] เขียนตัวตรวจ schema ที่รันตอน build และ fail ถ้าข้อมูลผิด
- [x] ใส่ท่าระดับ 1–5 ดาว ครบ 57 ไฟล์
- [x] ฟิลด์ `mirror` บอกว่าท่าไหนมีฝั่งตรงข้าม แล้วคำนวณฝั่งนั้นเอาเองด้วย `mirrorSteps` ไม่เก็บซ้ำ

**ปริมาณโดยประมาณ** สกิลมูฟราวแปดสิบท่า

**ความคืบหน้า (16 ก.ย. 2026)** ใส่สกิลมูฟแล้ว 57 ไฟล์ (รวมคู่ซ้าย/ขวาเป็นไฟล์เดียว เทียบได้ราว 80 ท่าในคู่มือ) ยืนยันครบทุกไฟล์ · 22 ท่าติดธง `mirror` · ยังไม่ใส่ท่าเดาะบอลกลางอากาศ 5 ดาว (มีแหล่งเดียว)

**ท่าที่เคยรอตรวจ (16 ก.ย. 2026)** `drag-back` `heel-flick` `roulette` `ball-roll-cut-sideways` `spin` ยึดตาม [fifauteam FC 27](https://fifauteam.com/fc-27-skill-moves/) ตามที่เจ้าของโปรเจกต์กำหนด แหล่งที่สองคือหน้า FC 26 ของเว็บเดียวกันซึ่งตรงกันทุกท่าทั้งดาวและปุ่ม ผ่านตัวตรวจสองแหล่งก็จริง แต่ไม่ใช่สองแหล่งอิสระ · `spin` เปลี่ยนเป็นค้างไหล่ขวาอย่างเดียว หมุน 270° และติด `mirror` เพราะหน้านั้นมีทั้งซ้ายและขวา · เอา `trickster-rainbow` ออก หน้าเดียวกันระบุว่าถูกตัดใน FC 27 ไว้ค่อยเพิ่มทีหลัง

---

## ระยะที่ 5 — ประกอบหน้า

- [x] `/` หน้าแรก ชวนเลือก preset จอยก่อนเริ่มใช้
- [x] `/skills` ภาพรวมทุกดาว
- [x] `/skills/[slug]` หน้ารายท่า
- [x] ช่องค้นหาและตัวกรองตามดาวและความยาก ค้นได้ทั้งสองภาษาพร้อมกัน
- [x] ลิงก์ท่าต่อเนื่องในหน้ารายท่า

**ยกเลิก** `/skills/1-star` ถึง `/skills/5-star` — ตัวกรองบนหน้า `/skills` แทนได้ทั้งหมดและกรองซ้อนกันได้ (ดาว + ความยาก + คำค้น) การแตกเป็นห้าหน้าจะได้หน้าโล่งสามหน้าและหน้าละยี่สิบท่าอีกสองหน้า

**เสร็จเมื่อ** เดินอ่านจากหน้าแรกไปถึงทุกท่าได้ โดยไม่เจอลิงก์ตาย ✅

---

## ระยะเพิ่ม — ฝึกกดจากจอยจริง

ไม่ได้อยู่ในแผนเดิม เพิ่มระหว่างทาง (16 ก.ย. 2026) ทำหลังระยะ 5 ฝั่งสกิลมูฟ

- [x] `input-sample.ts` อ่านค่าจอยเป็นฟังก์ชันบริสุทธิ์ แปลงแกนอนาล็อกเป็นหนึ่งในแปดทิศ
- [x] หน้าจอส่องสัญญาณจอยใน `/settings` บอกว่าเว็บเห็นอะไรจากจอย รวมค่าแกนดิบ
- [x] `recognizer.ts` ตัดสินว่ากดถูกไหม แยกจาก React และ `navigator` จึงทดสอบด้วยเฟรมสังเคราะห์ได้โดยไม่ต้องมีจอย
- [x] รองรับ `tap` `hold` `flick` `hold-stick`
- [x] รองรับ `rotate` จับแบบ subsequence เพราะอนาล็อกจริงกวาดผ่านทิศกลางเสมอ
- [x] `MoveTrainer` บนหน้าท่า ไฮไลต์จังหวะที่กำลังรอ
- [x] เคารพการผูกปุ่มของผู้เล่นผ่าน `physicalFor` โดย recognizer ไม่ต้องรู้จัก store
- [x] ท่าที่มี `mirror` ทำฝั่งไหนก็นับว่าถูก
- [x] ลอง `TIMINGS` กับ `STICK_DEADZONE` กับจอยจริงแล้ว ค่าที่ตั้งไว้ใช้ได้ ไม่ต้องจูน

**เสร็จเมื่อ** ทำท่าตามที่เว็บบอกบนจอยจริงแล้วเว็บบอกว่าถูก ✅

---

## ระยะที่ 6 — เก็บงาน

- [ ] ตรวจการแสดงผลบนมือถือจริง ไม่ใช่แค่ย่อหน้าต่างเบราว์เซอร์ (หน้าท่าผ่านแล้ว เหลือหน้าแรก รวมท่า ตั้งค่า และ `/about`)
- [x] ใช้งานด้วยคีย์บอร์ดได้ มี focus ที่มองเห็น — ทุกปุ่มเป็น `<button>`/`<a>` จริงอยู่แล้ว เพิ่ม: ช่องค้นหาไม่ปิด outline แล้ว, ปุ่มบนภาพจอยวาดขอบเองเพราะ Safari ไม่วาด outline บน SVG, `scroll-padding-top` กันแถบบนบัง
- [x] ตรวจ contrast ของสีทั้งธีมสว่างและธีมมืด — ตัวอักษรผ่าน 4.5:1 ทุกคู่อยู่แล้ว แก้สองจุด: ขอบช่องกรอกและปุ่มสลับใช้ `--control` (3:1 ตาม WCAG 1.4.11) และจุดกองหลังบนสนามได้ขอบขาว (แดงบนเขียวต่างกันแค่ 1.5:1 และคนตาบอดสีแดงเขียวแยกไม่ออก)
- [x] ใส่ title, description, og image ของแต่ละหน้า ทั้งสองภาษา — `pageMetadata` ใน `src/lib/metadata.ts` ใส่ canonical, hreflang, Open Graph และ Twitter card · รูปการ์ดสร้างตอน build ที่ `og.png` (ภาษาละใบ + ท่าละใบ) ใช้ route handler แทน `opengraph-image` เพราะแบบนั้นได้ไฟล์ไม่มีนามสกุล GitHub Pages จะส่งเป็น octet-stream · ฟอนต์ Kanit TTF อยู่ใน `src/assets/fonts` พร้อม OFL
- [x] ป้ายบอกว่าข้อมูลอ้างอิง EA FC เวอร์ชันไหน แสดงในทุกหน้าเนื้อหา — มีอยู่แล้วในหน้าแรก หน้ารวมท่า และหน้ารายท่า ตอนนี้กดแล้วไปหน้าที่มาของข้อมูล
- [x] หน้าอธิบายที่มาของข้อมูลและวิธีแจ้งแก้ไข — `/about` ลิงก์จากท้ายเว็บและป้ายข้อมูล รายชื่อแหล่งนับจากไฟล์ท่าตอน build
- [x] ตรวจว่า static export ไม่มีหน้าไหนพังเพราะ dynamic route — ไล่ลิงก์ภายในทุกหน้าใน `out/` 4,419 ลิงก์ ไม่มีลิงก์เสีย
- [x] อัป GitHub Actions เป็นรุ่นล่าสุด (checkout@v7, setup-node@v7, upload-pages-artifact@v5, deploy-pages@v5) ตัดคำเตือน Node 20 · upload-pages-artifact ตั้งแต่ v4 ทิ้ง dotfile จึงเปิด `include-hidden-files`

---

## ยืนยันจากอุปกรณ์จริงแล้ว

สี่ข้อนี้ทดสอบจากเครื่องมือไม่ได้ ลองกับจอยและมือถือจริงแล้วผ่านทั้งหมด (16 ก.ย. 2026) เก็บตารางไว้เป็นทางตรวจถ้าวันหลังพัง

| เรื่อง | ลองยังไง | ถ้าไม่ผ่านให้ดูที่ |
|---|---|---|
| การหมุนอนาล็อก | เปิด `elastico` แล้วกดเริ่มฝึก | หน้าจอส่องสัญญาณใน `/settings` ว่าหมุนช้า ๆ แล้วทิศเปลี่ยนครบไหม → `STICK_DEADZONE` |
| รับทั้งสองฝั่ง | `reverse-stepover` หมุนจากซ้ายขึ้นบน | `mirrorSteps` กับ `variants` ใน `MoveTrainer` |
| สนามบนมือถือ | เปิดหน้าท่าจากมือถือ | `viewBox` ใน `PitchDiagram.tsx` |
| focus ring | กด Tab ไปที่ปุ่มเริ่มฝึก | `PRIMARY_BUTTON` ใน `MoveTrainer.tsx` |

---

## ตัดสินใจที่พักไว้

- **ฟิลด์ `twin`** — มี 12 ท่าที่ฝั่งตรงข้ามของมันเป็นท่าอื่นในคลังอยู่แล้ว เช่น `hocus-pocus ↔ triple-elastico`, `spin-flick ↔ tornado-spin`, `elastico ↔ reverse-elastico` ท่าพวกนี้ไม่ได้ติดธง `mirror` เพราะจะกลายเป็นวาดซ้ำกับหน้าที่มีอยู่ ถ้าจะทำให้ถูกต้องควรลิงก์หากัน ไม่ใช่คำนวณ
- **`advanced-heel-flick`** — มีฝั่งตรงข้ามจริงตามเรขาคณิต แต่ไม่มีแหล่งไหนพูดถึง จึงคง `mirror: false` ตามกฎสองแหล่ง (`spin` ได้ `mirror: true` แล้วจากแหล่ง FC 27)
- **ทิศอิสระ** — `directional-nutmeg` โน้ตบอกว่าปัดทางไหนก็ได้ แต่โหมดฝึกบังคับตามทิศในข้อมูล ถ้าจะแก้ให้ถูกต้องต้องเพิ่มฟิลด์บอกว่าทิศนั้นเป็นตัวอย่างไม่ใช่ข้อบังคับ

---

## ข้อควรทราบเรื่องข้อมูล

ข้อมูลชุดตั้งต้นเป็นของ EA FC 26 ที่ยืนยันได้ ไม่ใช่ FC 27 ท่าสกิลมูฟส่วนใหญ่มักไม่เปลี่ยนข้ามภาค เว็บจะขึ้นป้ายบอกผู้อ่านให้ชัดว่าอ้างอิงภาคไหน และโครงสร้างไฟล์ข้อมูลรองรับการอัปเดตเป็น FC 27 ทีหลังโดยไม่ต้องรื้อโค้ด

## ลำดับที่ต้องเรียงกัน

ระยะ 1 → 2 → 3 ทำตามลำดับแล้ว ระยะ 4 แทรกคู่ขนานกับระยะ 3 ระยะ 5 ทำได้เพราะ 3 กับ 4 เสร็จ ระยะเพิ่มต่อยอดจากระยะ 2 และ 3 ยืนยันกับอุปกรณ์จริงแล้ว ที่เหลือคือระยะ 6
